interface:
  display_name: "OpenClaw Task Router"
  short_description: "Route tasks to safe planner policies and executors"
  default_prompt: "Use $route-openclaw-task to classify this task and return a guarded routing decision."
