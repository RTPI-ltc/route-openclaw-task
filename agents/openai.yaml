interface:
  display_name: "Task Compass Skill"
  short_description: "Route tasks to safe planner policies and executors"
  default_prompt: "Use $task-compass to classify this task and return a guarded routing decision."
